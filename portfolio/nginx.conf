server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Pages change on every deploy; let the CDN/browser revalidate.
    location = / { add_header Cache-Control "no-cache"; }
    location ~* \.html$ { add_header Cache-Control "no-cache"; }

    # Clean URL for the chat page. /chat.html stays reachable so existing
    # links (and the CI smoke test) keep working.
    location = /chat {
        add_header Cache-Control "no-cache";
        try_files /chat.html =404;
    }
    location ~* \.(pdf|webp|png|jpg|svg|woff2)$ { add_header Cache-Control "public, max-age=604800"; }

    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    gzip on;
    gzip_types text/html text/css application/javascript image/svg+xml;
    gzip_min_length 1024;

    location / {
        try_files $uri $uri/ =404;
    }
}
